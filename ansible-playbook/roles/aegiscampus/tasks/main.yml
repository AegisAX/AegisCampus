- name: Change /etc/hostname
  hostname:
    name: "{{ hostname }}"

- name: Ensure ufw is installed on the machine
  package:
    name: ufw
    state: present

- name: Allow TCP 22 for SSH.
  ufw:
    rule: allow
    port: "22"
    proto: tcp

- name: Allow TCP 80 for AegisCampus.
  ufw:
    rule: allow
    port: "80"
    proto: tcp

- name: Allow TCP 443 for AegisCampus.
  ufw:
    rule: allow
    port: "443"
    proto: tcp

- name: Enable ufw.
  ufw:
    state: enabled
    policy: deny
  when: enable_ufw_firewall

- name: Update APT package cache.
  apt:
    update_cache: yes

- name: Upgrade APT to the latest packages.
  apt:
    upgrade: safe

- name: Ensure /etc/ssl/csr folder exists
  file:
    path: /etc/ssl/csr
    state: directory
    mode: "0755"

- name: Ensure /etc/ssl/private folder exists
  file:
    path: /etc/ssl/private
    state: directory
    mode: "0755"

- name: Ensure /etc/ssl/crt folder exists
  file:
    path: /etc/ssl/crt
    state: directory
    mode: "0755"

- name: Install specified packages.
  apt:
    pkg: "{{ install_packages }}"
    state: latest

- name: adding existing user '{{ aegiscampus_user }}' to group ssl-cert
  user:
    name: "{{ aegiscampus_user }}"
    groups: ssl-cert
    append: yes

- name: Ensure the cryptography Python package is installed
  pip:
    name: cryptography

- name: Generate an OpenSSL private key with the default values (4096 bits, RSA)
  openssl_privatekey:
    path: "{{ aegiscampus_ssl_cert_path }}"

- name: Generate an OpenSSL Certificate Signing Request
  openssl_csr:
    path: "{{ aegiscampus_csr_path }}"
    privatekey_path: "{{ aegiscampus_ssl_cert_path }}"
    common_name: "{{ aegiscampus_domain }}"

- name: Generate a Self Signed OpenSSL certificate
  openssl_certificate:
    path: "{{ aegiscampus_crt_path }}"
    privatekey_path: "{{ aegiscampus_ssl_cert_path }}"
    csr_path: "{{ aegiscampus_csr_path }}"
    provider: selfsigned

- name: Update postfix main.cf configuration file.
  template:
    src: main.cf.j2
    dest: /etc/postfix/main.cf
    backup: yes
    owner: root
    group: root
    mode: 0644

- name: Restart postfix.
  service:
    name: postfix
    state: restarted

- name: Ensure postfix is started and enabled at boot.
  service:
    name: postfix
    state: started
    enabled: yes

- name: get latest release info
  uri:
    url: "https://api.github.com/repos/AegisAX/AegisCampus/releases/latest"
    return_content: true
  register: latest_json_reponse

- name: Download latest AegisCampus .zip file.
  get_url:
    validate_certs: True
    url: "https://github.com/AegisAX/AegisCampus/releases/download/{{ latest_json_reponse.json.tag_name }}/aegiscampus-{{ latest_json_reponse.json.tag_name }}-linux-64bit.zip"
    dest: "/home/{{ aegiscampus_user }}/aegiscampus.zip"
    mode: 0755
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"

- name: Ensure aegiscampus user has permission for CRT file.
  file:
    path: "{{ aegiscampus_crt_path }}"
    mode: 0755
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"

- name: Ensure aegiscampus user has permission for SSL certificate.
  file:
    path: "{{ aegiscampus_ssl_cert_path }}"
    mode: 0755
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"

- name: Create directory for aegiscampus.
  file:
    path: "/home/{{ aegiscampus_user }}/aegiscampus_deploy"
    state: directory
    mode: 0755
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"

- name: Unzip aegiscampus file.
  unarchive:
    src: "/home/{{ aegiscampus_user }}/aegiscampus.zip"
    dest: "/home/{{ aegiscampus_user }}/aegiscampus_deploy"
    remote_src: True # File is on target server and not locally.
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"

- name: Change ownership of AegisCampus folder and files.
  file:
    path: /home/{{ aegiscampus_user }}/aegiscampus_deploy
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"
    recurse: True

- name: Ensure aegiscampus binary is executable
  file:
    path: /home/{{ aegiscampus_user }}/aegiscampus_deploy/aegiscampus
    mode: 744

- name: Ensure aegiscampus binary is allowed to bind to privileged ports using setcap
  capabilities:
    path: /home/{{ aegiscampus_user }}/aegiscampus_deploy/aegiscampus
    capability: cap_net_bind_service+eip
    state: present

- name: Copy config.json file.
  copy:
    src: files/config.json
    dest: "/home/{{ aegiscampus_user }}/aegiscampus_deploy/config.json"
    owner: "{{ aegiscampus_user }}"
    group: "{{ aegiscampus_user }}"
    mode: 0644

- name: Ensure aegiscampus service file is properly set
  template:
    src: aegiscampus.service.j2
    dest: /etc/systemd/system/aegiscampus.service
    mode: 644

- name: Ensure systemd to reread configs
  systemd:
    daemon_reload: yes

- name: Ensure aegiscampus is properly started
  service:
    name: aegiscampus.service
    state: started
    enabled: yes

- name: Ensure nginx is installed
  package:
    name: nginx
    state: present

- name: Ensure nginx service file is properly set
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
    mode: 644

- name: Ensure nginx service is restarted
  service:
    name: nginx
    state: reloaded
    enabled: yes

- name: get AegisCampus log file which contain initial password
  command: cat /home/{{ aegiscampus_user }}/aegiscampus_deploy/aegiscampus.log
  register: aegiscampus_log

- name: display log file
  debug:
    msg: "{{ aegiscampus_log }}"

- name: Reboot the box in 1 minute.
  command: shutdown -r 1
  when: reboot_box
